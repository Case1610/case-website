# AGENTS.md

複数の AI エージェント（Claude Code / Codex など）が共通で読む約束ごと。

**リポジトリ固有の文脈は [CLAUDE.md](./CLAUDE.md) にある。先にそちらを読むこと。**

ここには、どのエージェントが読んでも同じ意味になることだけを書く。
リポジトリの事情をここに書き写すと CLAUDE.md と二重管理になり、必ず片方が古くなる。
実際 2026-09-12 時点で、別のリポジトリの AGENTS.md が CLAUDE.md の古いコピーになっており、
本人の経歴・情報公開の判断基準・参照すべきファイルの存在の3点がずれていた。
**同じことを2箇所に書かない。** 片方は必ずもう片方への参照にする。

---

## 作業の単位は Issue

- **1 セッション = 1 Issue**。Issue に書かれた範囲だけをやる
- 範囲外の問題を見つけたら、直さずに新しい Issue を立てる。
  「ついでに直す」はセッションを膨らませ、後から何が起きたか追えなくする
- Issue のない依頼を受けたら、先に Issue を立ててから着手する
- 着手したら自分を assign する。assign 済みの Issue には手を出さない

セッションを短く保つことが目的である。
長いセッションは、文脈をリポジトリではなく会話の中に溜め込む。
その会話が終わった瞬間、溜めたものは失われる。

## 終わる前に必ず書き戻す

次のセッションはこの会話を読めない。別のエージェントならなおさら。
**判断は会話ではなくリポジトリに残す。**

1. 決めたことを `docs/decisions.md` に追記する（無ければ相当する置き場へ）
2. 現在地を `docs/status.md` に反映する（無ければ相当する置き場へ）
3. Issue にコメントするか、閉じる

これを守っている限り、セッションは短くてよい。
文脈を持つのはセッションではなくリポジトリの側である。

## 人間に聞くこと・聞かないこと

**聞く**

- 本人の立場、社内の状況、人間関係。推測で補わない
- 何を公開するかの線引き
- 原則や方針そのものを変える判断

**聞かない**（自分で決めて進め、決めた理由を残す）

- 実装の手段、命名、ファイルの置き場所
- 既に決まっている原則から導ける判断

本人は白紙から書くのが続かない。
選択肢を並べて丸投げするのではなく、下書きを出して直してもらう形にする。

## 直接 push してよい

これは一人のためのリポジトリ。
PR とレビューを必須にすると、止まるのは他人ではなく本人だけになる。
`main` / `master` へ直接 push してよい。

ただし上の「終わる前に書き戻す」は飛ばさないこと。これが唯一の歯止めである。

## AI が書いたものには、そう分かる印を付ける

GitHub の API は本人の資格情報で動くため、**エージェントが立てた Issue も、書いた
コメントも、作者は本人として記録される。** 別の作者名で残す手段は今のところない。
だから印は自分で付ける。

- **Issue を立てたら `ai-drafted` ラベルを付ける。** 無ければ自動で作られる
- **Issue の本文の末尾に、誰が書いたかを1行書く**（例: `> このIssueはClaude Codeが下書きしました`）
- 本人が中身を確認して手を入れたら、ラベルを外すか `reviewed` を足す。
  「AIが書いた」と「本人が確認した」は別の事実であり、後者の方が重要

理由は2つある。

1. 後から読む人（他のエージェント、将来の本人、第三者）が、その文章の
   確からしさを測れるようにするため。AI の下書きは調べた範囲でしか正しくない
2. 本人が言っていないことを本人の名前で残さないため。
   帰属の偽装は、たとえ善意でも信頼を削る

## エージェントの役割分担

本人の判断（2026-09-12）: **GitHub App（`@claude` / `@codex`）はレビュー用と提案用。**
実装を駆動する主体にはしない。

| 呼び方 | 役割 | 向いている仕事 |
|---|---|---|
| クラウド実行セッション（claude.ai/code など） | **駆動** | リポジトリをまたぐ作業、判断が要る作業、本人との対話、デプロイに触る作業 |
| `@claude` / `@codex`（Issue・PR でのメンション） | **レビューと提案** | 差分へのレビュー、Issue への案出し、見落としの指摘 |

分けている理由は能力差ではなく、**判断の記録が散らないようにするため**。
判断が要る作業を複数のエージェントに分散させると、決めたことが複数の場所に散り、
どれが正かが分からなくなる。このリポジトリ群では、同じ形の事故が既に3回起きている
（`AGENTS.md` の写し、Secret のズレ、`profile.sample.json` の二役）。

レビューと提案は、判断を**増やす**のではなく**材料を増やす**行為なので、
複数いても壊れない。むしろ視点が増える分だけ良くなる。

したがって:

- レビュー・提案のメンションに、**実装して push してよいかを勝手に判断させない**
- 提案が採用されたら、駆動側が実装し、判断を `docs/` に残す
- レビュー指摘が正しくても、範囲外なら直さず新しい Issue を立てる（上記「作業の単位は Issue」）
